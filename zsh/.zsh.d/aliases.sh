# Prefer GNU tools over BSD ones
alias sed="gsed"
alias awk="gawk"

# Editor & git
alias n="nvim"
alias g="git"
alias ga='git add . && git commit -m'
alias gh="git push origin head"
alias tm=new_tmux
